# Static HTML site (index.html, app.js, styles.css, assets/) — no Vite build.
[build]
  publish = "."

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
